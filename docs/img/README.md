# Screenshots (optional)

Not referenced by the top-level README — add them and wire them up if you want.
Broken image links look worse than no images, so the README ships without them.

If you do add them, crop out browser chrome and take them after running
**Extract arguments**, so provision chips are clean.

| Suggested file | What to capture |
| --- | --- |
| `ledger.png` | The **Ledger**, sorted by significance, showing linked rows with Federal Register pages and accepted/partial/rejected verdicts |
| `metrics.png` | The top of **Metrics** — the stat tiles and the four-way outcome grid, including the base-rate warning band |
| `silent-grants.png` | The **Silent grants** table, with the sections that moved and their magnitudes |

Then add a section to the top-level README:

```markdown
## What it looks like

<p align="center"><img src="docs/img/ledger.png" width="880"></p>
```
